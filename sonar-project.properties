# Organization and project keys are displayed in the right sidebar of the project homepage
sonar.organization=gegelati
sonar.projectKey=gegelati_gegelati

# --- optional properties ---

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=.

# Exclude tests from analysis
sonar.exclusions=test/**,lib/**,scripts/**

# Path to compilation database
sonar.cfamily.compile-commands=bw-output/compile_commands.json

# Coverage results generated by gcovr
sonar.coverageReportPaths=bin/sonarqubecleaned.xml

# Enable cache
# Check https://docs.sonarsource.com/sonarcloud/advanced-setup/languages/c-c-objective-c/#analysis-cache

# Use the two threads from travis
sonar.cfamily.threads=2
